{
  "name": "quay-oidc-auth-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action for OIDC federated authentication against Quay",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run build && npm test && npm run package"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/http-client": "^2.2.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24.0.3",
    "@vercel/ncc": "^0.38.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "typescript": "~5.8.3"
  }
}
